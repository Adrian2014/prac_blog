<h1></h1>

<%= link_to 'New article', new_article_path %>

<!-- <table>
  <tr>
    <th>Title</th>
    <th colspan="3"></th>
  </tr>
 
<% @articles.each do |article| %>
  <tr>
    <td><%= link_to article.title, article_path(article), class: "button small [radius round]" %> | </td>
    <td><%= link_to 'Edit', edit_article_path(article), class: "button small [radius round]" %> | </td> 
    <td><%= link_to 'Destroy', article_path(article),
                    method: :delete, data: { confirm: 'Are you sure?' }, class: "button small [radius round]" %></td> 
  </tr>
<% end %>
</table> -->



<section id="articles">
    <div class="row text-center full-width">
      <div class="medium-10 columns small-centered">

        <div class="row">
          <div class="small-4 columns">
            <h1>Hypotheses</h1> 
          </div>
        </div>
<% @articles.each do |article| %>

        <div class="row_article">
          <div class="small-4 medium-4 large-4 columns">
            <%= link_to article.title, article_path(article), class: "button small [radius round]" %>
          </div>
          
          <div class="small-4 medium-4 large-4 columns">
            <%= link_to 'Edit', edit_article_path(article), class: "button small [radius round]" %> 
          </div>

          <div class=" small-4 medium-4 large-4 columns">
            <%= link_to 'Destroy', article_path(article),
                    method: :delete, data: { confirm: 'Are you sure?' }, class: "button small [radius round]" %>
          </div>
        </div>

<% end %>
      </div>
    </div>
  </section>